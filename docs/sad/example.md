## 项目路径（如在本机）、一句话目标、栈与形态、你现在脑中的模块/目录划分（现状即可），以及印象里最疼的 2–3 次跨模块改动或 bug——最后这条是判据压测的最佳入口

项目在 /Users/cheneyzhao/Documents/05-sarvelo/mqtt-console，Sarvelo MQTT Console 是一个 local-first MQTT command
workspace：Wails v2 桌面应用，Go 后端 + Svelte 5 前端，纯文件存储（YAML + JSONL），无数据库、无云依赖。同一个二进制有两种形态——默认开 GUI 窗口，带 run 子命令则跑 headless harness

/