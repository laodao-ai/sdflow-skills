sdflow 工作流优化调研结论 + 分阶段 Roadmap

总体判断（TL;DR）

不需要重构，需要「收束 + 执行 + 校准」：仓里其实已经沉淀了一份高质量的优化提案（docs/sdflow-fable5/04-optimization-proposal.md，2026-07-10，22 项建议全部入池为 T97–T128 并定过批次优先级），且两条既有 roadmap（成本优化、机械层固化）都停在「已规划、待起手」的节点。当前真正的问题是三个：

1. 在途工作乱了：mlh-p6 有两条平行血脉都没并回 main，werror CI 门只有裸脚手架叠在旧血脉上——不先收束，后续一切排期的状态口径都会分叉。
2. 最大的单点浪费在跑：模型档位是 advisory 未强制——model-tiers.md 写着机械镜用 light/mid，实际 fan-out 子代理不带 model=、全部继承主 session 的 Opus 4.8。每轮评审 5–6 面镜全按最贵的档在烧。
3. 砍不砍镜的数据已到阈值、判据还没建好：9 面镜出现轮数 ≥10 触发待复评，但砍留所需的实修率（resolution rate）和 token 维度量还没落地，现在拍板只能拍感受。

数据底座（本次实测）

retro 报告已再生（openspec/retro/report.md，未提交，改动留给你随后续流程一起 commit）。按 D12 契约显著呈现：

▎ ⚠️ 待复评（出现轮数 ≥10，只提示、不判断、不自动砍）：code-review 层 adversarial(18)/broad(14)/domain(17)/history(17)/outside-voice·codex(19)；spec-review 层 adversarial(14)/broad(14)/grounding(14)/outside-voice·codex(13)

关键数字（40 change / ~331 hr 墙钟 / 22 个带真锚）：

事实: 墙钟大头
数字: impl 17%、spec-review 13%、ff 9%、code-review 仅 2%
含义: 评审墙钟不是大头——降档省的是钱（token），不是时间（wco roadmap P0 已实测证伪"降档省墙钟"）
────────────────────────────────────────
事实: unknown 桶
数字: 56%
含义: 几乎全被离群 change scoped-test-per-task（165.5 hr、仅 3 个 checkpoint）拉爆——归因质量本身是债
────────────────────────────────────────
事实: 产出最强镜
数字: code-review adversarial：144 条 / 采纳 78% / 独立 56%；code-review outside-voice(codex)：70 条 / 采纳 89%
含义: 对抗镜和跨模型 code-voice 是真承重墙
────────────────────────────────────────
事实: 产出偏弱镜
数字: code-review history：17 轮仅 13 条、独立 23%；code-review broad：14 轮 12 条；spec-review broad 独立
20%；spec-review design-voice(codex) 独立仅 14%、被裁 23 条全表最高
含义: 阶段 3 复评的候选焦点（只列数据，砍留
────────────────────────────────────────
事实: 成本双峰
数字: 小 change 评审占比可达 100%（gate-anchor-line-scoped），大 change 仅 0–5%
含义: 微变更快速通道（T100）的立项实证
────────────────────────────────────────
事实: issues 池
数字: 未决 118 项（1 bug + 117 todo），其中 opt-* 优化批 31 项已定级
含义: 优化工作大半已登记，缺的是排期执行

可优化点

已入池待执行的主干（不重复罗列，按既有批次）：
- opt-p0（P0，6 项）：T97 档位强制落地、T98 prompt 前缀缓存、T99 确定性检查前置准入门、T100 微变更三档通道、T101 设计门三层摘要头、T102 对抗镜措辞收紧
- opt-cost（P1，4 项）：T103 每镜 effort 预算+输出封顶、T104 retro 补 token 维、T105 thinking 按步分档、T124 规则注入分界
- opt-review-reliability（P1，9 项）：T106 裁决二元化、T107 位置去偏、T108 实修率指标、T109 测试大改红旗、T110 决策原则明码、T112 弱档 validator 复核、T113 HIGH 跨模型双栈等
- mlh-p4-target-state（P1，7 项）：机械层 P4 余项（T79–T82 脚本化）+ P6（T85，即在途的 mlh-p6）
- opt-structure（P2，7 项）：T117 跨工件一致性 lint、T115 EARS 模版、T118 tasks DAG 并行等

本次调研新发现（池子里没有的）：
1. 在途乱局：mlh-p6 旧血脉（已实现+验证绿+归档，领先 main 49 commit）vs 新 -ship 血脉（刚过设计门、0/33 未动工）并存；add-werror-ci-gate 仅 .openspec.yaml 裸脚手架；另有 3 条已并入 main 的 stale 分支；T155/T153/T154 只存在于分支，main 台账没有。
2. retro 归因质量债：离群 change 无边界截断（unknown 56%）、30 份旧格式报告无锚、独立率跨轮口径漂移——度量回路要先可信，阶段 3 拍板才有据。
3. codex 宿主真负载 efficacy=0：A3 反向 outside-voice 在真实评审负载（300s+大 context）下 3/3 timeout，smoke 绿≠真绿；但这只影响 codex-host 方向，你的主循环（Claude 宿主 → codex voice）19 轮 89% 采纳、是真价值，不受影响。
4. 台账卫生：B7（Unicode 数字致裸 Traceback）仍 OPEN；B6 标 FIXED 但详情三栏还是占位；6 个批次优先级 <待填>；仓根 8 个 {"bugs": ...} 畸形测试残留目录（已实地核实存在）。

优化 Roadmap（6 个阶段，每阶段 → 1–2 个 change）

阶段 0 · 收束与地基（先决条件，本周可完）

目标：main 恢复单一真相；CI 门在位，守卫从「手动 pytest」变「push 即验」。
- mlh-p6 血脉拍板并落地其一（见下方待拍板 #1）→ T85 闭环
- 做完 add-werror-ci-gate（T88 落地：pytest + -W error + 托管块/单一源守卫全上 CI）——CI 是后面所有机械化阶段的放大器，故排最前
- 台账卫生：B7 修复、B6 详情回填、6 批次定级、stale 分支与畸形目录清理（删除动作待你确认）
- 验收：git branch 只剩 main + 真在途；PR 上可见 CI 绿；issues reindex 一致。

阶段 1 · P0 速赢批（opt-p0 六项）

目标：token 账单与设计门人读负担双降，零质量回归。
- 核心是 T97 档位强制落地：fan-out 显式带档、失败 fail-closed（禁静默回落 opus）；Opus 4.8 只留在铁律位置（verify 终门、Step3 裁决、主审）——这与「带门禁的步 MUST NOT 降档」完全兼容
- 配 T98 缓存前缀（三条通则 71 行 × 每子代理的重复注入是设计使然，但可以通过前缀稳定让它命中缓存）、T99 准入门、T100 微变更通道（直击成本双峰）、T101 摘要头、T102 措辞收紧（改一句话）
- 验收：全确定性判据 + 改前后各跑数个 change 用 retro 对照 token/采纳率/独立率。

阶段 2 · 度量升级 + 可靠性校准（opt-cost 余项 + opt-review-reliability）

目标：「评审值不值」从感受变数据；裁决可校准。
- 度量腿：T104 token 维、T108 实修率、retro 归因修正（离群边界截断 + 独立率口径校验，新登记 todo）
- 可靠性腿：T106 二元裁决、T107 位置去偏（仅 HIGH）、T110 决策原则明码、T112 validator 复核、T113 HIGH 双栈、T109 红旗规则、T7 决策登记必填
- 验收：retro 报告新增 token 列 + 实修率列；裁决结构改版后采纳率不降。

阶段 3 · 镜组合复评（人门事件，非 change）

目标：9 面待复评镜的保留/降采样/淘汰有数据拍板，解除「禁砍任何镜」冻结。
- 输入 = 阶段 2 的实修率 + token 维 + 校验过口径的独立率；候选焦点即上表「产出偏弱镜」四位
- 产出 = 镜 roster 配置化 change（若拍板有变动）。砍留永远归人——这是 lens-metric 回路的 user-sovereignty 设计，roadmap 只保证「拍板时手里有数」。

阶段 4 · 机械层收尾（可与阶段 2 并行，独立腿）

目标：adr/0006(b) 收口——机械 prose 全下沉；假绿/fail-open 同型失效面（adr/0018 家族）闭合。
- T79（maintain set-diff）单开；T80/T81/T82 三合一；validators 残差 T136（anchor_lint 重算交集堵手改绕过）/T138/T139/T140、T148
- 依托阶段 0 的 CI，全部守卫 push 即验
- ◐ 项（T83/T84）维持「待 embedded producer 契约」不动——这是合法 defer（缺依赖模块），非现状妥协。

阶段 5 · 结构增强与并行化（观察触发，不排死期）

目标：墙钟的下一档收益 + 跨宿主承诺明确化。
- opt-structure 批（T117 跨工件 lint 优先，其余按 P2 节奏）
- wco-P3 接地镜流水线（前置：阶段 1 的档位落地）；wco 阶段 C tasks frontier 并行（硬前置：matt tickets 试点判赢）
- codex 宿主二选一：修 A3 真负载 timeout（上调超时/context 瘦身 + T150 真探针 + T6 双侧 hook），或显式写下降级承诺（codex-host 仅 smoke 级支持）。推荐后者先行——你的主力是 Claude Opus 4.8，claude→codex 单向 voice 已验证够用，codex-host 深投入 ROI 目前最低。

排序逻辑一句话：CI 放大器最先（阶段 0）→ 最大单点省钱（阶段 1）→ 先建尺再校准（阶段 2）→ 有尺才拍板（阶段 3）；机械层独立并行（阶段 4）；墙钟大招按触发条件启动（阶段 5）——对远期阶段刻意保持 fog-of-war，不假精确。

需要你拍板的（只有这几件在你脑子里）

1. mlh-p6 两条血脉取哪条：旧血脉已实现+验证绿，新 -ship 血脉刚过设计门 0/33。重启原因仓里查不到。我的默认建议：diff 两版 design，若新版无实质增量 → 并旧血脉、关新壳；若重启是因为旧实现有你知道的问题 → 以新设计为准、旧实现按 task 拣回。
2. 8 个畸形 {"bugs": ...} 目录是否删除（测试残留，我按安全规则不擅删）。
3. codex 宿主走「修」还是「显式降级承诺」（我推荐先降级承诺，理由如上）。
4. 是否把本 roadmap 用 /sdflow-roadmap 固化成 openspec/roadmaps/ 三件套——它跨 6 个阶段 10+ 个 change，超出单次 change 层级，正是该 skill 的定义域；固化时可顺带完成两条旧 roadmap 的状态归并。你点头我就跑。

✻ Churned for 15m 0s

※ recap: 调研 sdflow 工作流优化点并出分阶段 roadmap，已完成：六阶段方案（收束在途、CI 地基、档位降本、度量校准、镜复评、机械层收尾）。下一步等你拍板 mlh-p6 血脉取舍及是否用 /sdflow-roadmap 固化。 (disable recaps in /config)




## 这个是用 当前 repo 本身的 skill 来评估的，不用当前 repo 的 skill ，重新选择 skill 来评估一次

好——上一轮的定量底座用了本仓自己的 sdflow-retro（自评有自指偏差），这次换仓外第三方 skill 做独立评估。选择与理由：

1. autoplan（gstack）——自动跑 CEO / design / eng / DX 四镜评审管线，带 6 条自动决策原则，非交互可全程自主：把「sdflow 工作流定义 + 既有优化提案」当作被审对象，从四个独立视角挖可优化点
2. health（gstack）——代码质量仪表盘：独立评估仓内 Python 脚本/测试的工程质量，替代上一轮「自家 checklist」视角
3. 上一轮的客观事实（git 墙钟数字、issues 台账、归档记录）是数据不是观点，保留复用；判断层全部换外部镜，最后对照两轮结论出修订版 roadmap

先调 autoplan：

⏺ Skill(autoplan)
Successfully loaded skill · 8 tools allowed

Made 2 scratchpad edits +110 -1, read 1 file, ran 3 shell commands

⏺ Web Search("AI agent code review workflow cost optimization model cascading 2026 best practices")
Did 1 search in 17s

Made 1 scratchpad edit +76 -1

Phase 1 · CEO Review — Step 0 完成，到达前提门

Step 0 全量分析已落盘到计划文件（0A–0F + Landscape + Retrospective + 决策审计 8 条）。给你划三个 load-bearing 发现：

1. 前提挑战只咬住一条：PR-4「砍镜必须先补齐判据」——对不可逆淘汰成立，但降采样是双向门（改回来只需一行 config），按可逆性分类不必等完美数据。「先可逆降采样、后数据定砍留」被标为 User-Challenge 级，因为它触碰你既定的 user-sovereignty 方向，留终门由你拍板。
2. Retrospective Check 抓到架构气味：近 30 天返工热区全是 prose 契约文件（ship_gate.py 38 次、两个评审 SKILL.md 各 33-34 次改动）——「链序措辞不许漂」的字符串耦合是返工根因，Eng 相会对此加压。
3. 一个未验证的可行性风险：T104「retro 补 token 维」的数据源（per-subagent token 是否可得）从未做过 spike——如果拿不到镜级粒度，阶段 2/3 的「按镜算账拍板」判据会降级为 session 级总量。

按 autoplan 规则，前提确认是唯一不可自动决策的门（AskUserQuestion 工具不可用，按协议走 prose 形式）：

---
D1 — 确认评审前提（6 条）
Project/branch/task: sdflow-skills @ main，autoplan 正在审「工作流优化 roadmap v2」计划。

ELI10：后面所有深审（CEO 10 节 + Eng + DX + 双模型对抗声）都会以这 6 条前提为地基。前提错了，深审做得再细也是在错误方向上精修——这就是为什么它是唯一必须人确认的门。

前提清单（完整论证在计划文件 0A 节）：
- PR-1 主力 = Claude Code + Opus 4.8，Codex 只作第二意见 runner ✅
- PR-2 真痛点 = token + 人读负担，不是墙钟（引用数字将统一为新 40-change 口径）✅
- PR-3 不推倒重构 ✅
- PR-4 砍镜留人拍板、先补判据 ⚠️ 被部分挑战：可逆降采样是否可以先行（见 E1，终门另问）
- PR-5 CI 最先做（守卫放大器）✅
- PR-6 一阶段 = 1-2 个完整 change ✅

Stakes if we pick wrong: 前提定错 → 后面三相深审 + 双模型声全部在错误基线上花掉。

Recommendation: A，因为 6 条前提有 retro 实测、归档实证、外部对标三路证据交叉支撑，PR-4 的争议点已单独拆出留终门，不阻塞前提确认。

Note: options differ in kind, not coverage — no completeness score.

A) 确认全部 6 条前提，继续深审（recommended）——前提即地基，全部有据；PR-4 的降采样争议已拆成独立决策留终门，不需要现在解决。风险：若你脑中有我看不到的组织约束（如近期预算/时间硬线），它不会被审进去。

B) 修改某条前提——回复如「B：PR-2 改为…」。深审将基于修改后的前提跑，成本是本轮多一次往返。

C) 增补前提——你知道而仓里查不到的约束（偏好、踩过的坑、时间线）。这正是人门存在的意义。

Net: 用一次确认换后面全部深审的方向正确性。

回复字母即可（A / B：具体修改 / C：增补内容）。确认后我继续：双模型对抗声（Claude 子代理 + Codex CLI）→ CEO 11 节深审 → Eng 相 → DX 相 → 终门汇总。


/autoplan 评审完成 — 终门（外部 skill 独立评估 · 第二轮结论）

四相管线全部跑完：CEO / Eng / DX 三相 × 每相双声（独立 Claude 子代理 + Codex CLI 真跨模型），Design 相无 UI scope 跳过。总计 45 条 findings（6 条经原文核验为阻断/高危），28 条聚合任务已落盘。全档产物在计划文件 scratchpad/sdflow-optimization-roadmap-plan.md + ~/.gstack/projects/laodao-ai-sdflow-skills/（任务 JSONL×3、测试计划、评审日志）。

一句话总裁决

第一轮 roadmap 的骨架方向被双模型确认（收束→CI→数据→拍板），但排序和承诺强度被推翻：两个模型跨三相一致认定「先改档位、后补度量」是因果倒置，且第一轮引用的成本数字（code-review 2%/spec-review 13%）在归因算法层面就不可识别——不只是离群值问题。

已核验的关键推翻性证据（原文锚，非观点）

1. 归因算法反向：retro_report.py:204 注释原文「相邻提交差计入前一个提交的阶段」，但 checkpoint 是阶段完成锚 → 区间时间属于下一阶段。Codex 重算同批 36 change：code-review 2%→11.4%，spec-review 13%→8.1%。第一轮据此排的优先级地基是坏的。
2. T97「档位强制」只能覆盖半场：主 session 强档位（裁决/主审——最承重的门禁位）SKILL 原文自认「是对主 session 的指令、非机械门」，无任何执行/审计手段。本次评审 session 本身跑在 Fable 而非 opus，就是漂移无人察觉的实证。
3. resolver 契约与计划相反：unknown → exit 0 + 默认档位，被 test_resolve_models.py:124 锁死；「unknown→REFUSE」是协议变更不是接线，且 Codex 在自己环境实跑出双信号冲突场景——直接开 REFUSE 会锁死现存正常会话。
4. 单一源纪律已在漏水：openspec/workflow/tools/ 与 sdflow-init/assets/workflow/tools/ 三个脚本当前实际漂移（diff 实测非空）——「只缺 CI 壳」不成立，阶段 0 须升格为 release blocker 级 reconcile。
5. ship_gate 可绕过一切新审计：只认 3 个 frontmatter 字段，手写 pass 即通过。
6. DX 总分 4/10：新门家族（REFUSE/lint/准入门）普遍缺三件配套——缺键语义分治、脚本级错误信息（what/why/fix）、显式留痕逃生门。

User Challenges（双模型一致建议改变你已确认的方向 — 按协议你的原方向默认保留）

UC-1：PR-2 前提的数字地基失效
你确认过：「真痛点=token+人读，不是墙钟（code-review 仅 2%）」。
双模型建议：方向暂持、数字撤回——token 痛点依然结构性成立（镜全跑 opus 是静态事实），但所有阶段占比数字在归因修复前不得用于排期。
可能遗漏的上下文：你对实际账单的直接感受（API 按量还是订阅制？全计划无人声明）。
若我们错了代价是：多花一个「归因修复+token spike」前置 change（~1-2 天），换来的是可信基线。

UC-2：PR-4「砍镜先补判据」
你确认过：砍镜留人、拍板前必须补齐实修率+token 判据。
双模型建议：不可逆淘汰维持原则；但可逆降采样（如 history 镜 17 轮仅 13 条、独立率 23% → 降为 1/3 抽样）是双向门，可先行且随时翻回。
可能遗漏的上下文：你留 9 面镜全跑可能有我看不到的保险考量（如正处高风险变更期）。
若我们错了代价是：抽样期漏掉该镜本会抓到的 1-2 条独立 finding（历史数据显示该镜独立产出率最低）。

主 taste 决策（双模型真分歧，需要你拍）

D2 — 修订推进 vs 收缩实验（Note: options differ in kind — no completeness score）

A) 路线 A 修订版（Claude 声方向，recommended）：保留六阶段承诺，但按 28 条任务修正——阶段 0 扩容吸收全部 P1 前置（归因修复、token spike+计费声明、拷贝 reconcile、supersession 回写、mlh-p6 拍板），T97 改 opt-in flag + 逃生门 + 诚实措辞（派发侧机械守/主 session 标 unverified），锚 schema v3 一次成型（tier+关联键+granularity），「新门 DoD 三件套」立为常备规则。
✅ 保持 roadmap 的承诺性与 issues 池的排期归属；每步可独立交付回滚。
❌ 在自指数据修好之前，阶段 2/3 的价值仍靠源仓单点样本支撑。

B) 30 天实验版（Codex 三相一致方向）：只承诺 4 项（血脉收束+最小 CI、归因/token 数据源修复、replay 盲测集≥3 消费仓+seeded faults、roster 配置机制），盲比三套 roster（全镜 / 机械+单强审 / 对抗镜+codex voice）+ 原生 /code-review 基线，达非劣阈值才逐项部署 T97/T98/T100/T102。
✅ 每个后续投入都有对照证据；直接回答「这套工具值不值得继续维护」。
❌ 30 天内没有任何成本改善落地；replay 集建设本身 ~3 天+且需要 ≥3 个消费仓配合（当前只有 1 个活跃消费仓，这条前置在今天可能根本不可满足——这是我认为 B 最实的软肋）。

Recommendation: A，理由：B 的核心洞见（先度量、对照、退役条件）已全部折进 A 修订版的阶段 0/2 与常备规则；而 B 独有的「≥3 消费仓 replay 盲测」在当前单消费仓现实下条件不成立，会让整个 roadmap 挂起在一个外部依赖上。

次级 taste 决策（回复覆盖即可，不回复按推荐执行）

- D3 tier 审计源：hook vs transcript 后处理（推荐）——同源拿真实 model id+token，一石二鸟，且是「主 session 档位」唯一可机械审计的路径
- D4 E6 roster 配置化提前入阶段 2：推荐纳入（机制先行，阶段 3 变 config flip）
- D5 Codex 单声挑战 PR-6（阶段切 change 制造固定税）：推荐维持你的拆分纪律（单声、且 T100 微变更通道正面解决同一问题）
- D6 metrics.enabled/enforce 两档拆分：推荐做（消费仓数据回路的前提）

评分与共识

- CEO：6 提案 2 收 2 缓；双声共识 3/6 CONFIRMED、2 DISAGREE
- Eng：15 issues（2 critical）；共识 6/6 全维度 CONFIRMED 有缺口
- DX：4/10，TTHW 拆双指标（tool-ready P90<5min / 完整 cycle 单独度量）；共识 5/6+1 FLAGGED
- 跨相主题 6 条双模型确认：先度量后优化 · 新门 DoD 三件套 · 度量自指需外部对照 · 漂移+升级非原子=release blocker · 主 session 档位诚实边界 · 平台吸收/原生基线

聚合任务（28 条，去重后 27——T3 hook 被 E7 transcript 取代）

P1×11（归因事件模型、token spike 前置、锚 v3、resolver 协议 change、拷贝 reconcile、T97 措辞收窄、缺键三分治、错误 envelope、逃生门家族…）· P2×12 · P3×5——完整清单已在上文聚合输出与 JSONL 工件中，拍板后按 D2 选择映射进修订版阶段。

---
回复方式：A（按修订版路线 A 批准，我随后产出 roadmap v3 终稿）/ B（收缩为 30 天实验版）/ B2: UC-1=…, UC-2=…（对 User Challenge 逐条表态）/ C: <问题>（追问任一决策）/ D（计划本身要改）/ E（推翻重来）。次级决策 D3-D6 不回复即按推荐执行。




我推荐的下一步(带主次)

① 立即·无争议 ──  收束 mlh-p6 → main,恢复单一真相
├ merge feat/mlh-p6-recorder-frontmatter 回 main(6903行实现+测试+归档四件套)
│   ⚠ 会与我今天两个 docs 文件(20260712/20260717.md)小冲突,保留 main 版即可,无代码冲突
├ 删 -ship 书签 + 清 /private/tmp worktree(证明是祖先,删了零损失)
├ 删 3 条 stale 分支(codex-host/architecture/devenv,已全 MERGED)
└ 修 archive/ 空目录(要么补齐要么删,消除"假完成")
↓ 验收:git branch 只剩 main+真在途;openspec list 与 archive 口径一致
② 紧接·地基 ──  修尺子(autoplan 推翻性发现,必须先于"按占比排期")
├ 归因修复 retro_report.py + 重算 → 拿到可信阶段占比
└ 单一源三脚本 reconcile(升 release-blocker) + 做完 add-werror-ci-gate(CI 是后续放大器)
③ 方向拍板 ──  autoplan D2(见下),定了才谈 P0 降本批

真正需要你拍的(只有这几件在你脑子里,其余我都能自己定)

Q1 — mlh-p6 为什么"归档产物到了 main、6903 行实现却没并"?
这是流程中断的残影,还是你有意留着?我的默认:直接把完整线并回 main(技术上干净)。但如果重启 -ship/开 werror scaffold 时你脑中有"旧实现有问题要返工"的理由,现在告诉我——git 里查不到。

Q2 — autoplan D2 方向:A 修订版 vs B 30天实验版(昨天悬着没拍)
- A 修订版(autoplan 推荐,我也推荐):保六阶段,阶段 0 扩容吸收全部前置(归因修复+单一源 reconcile+mlh-p6 收束),T97 改 opt-in flag+逃生门+诚实措辞。每步可独立交付回滚。
- B 30天实验版:只承诺 4 项,建 ≥3 消费仓 replay 盲测集做对照。软肋:当前只有 1 个活跃消费仓,这条前置今天根本不可满足——会把整个 roadmap 挂在一个满足不了的外部依赖上。
- 我推荐 A:B 的核心洞见(先度量、对照、退役条件)已折进 A 的阶段 0/2;B 独有的东西现实条件不成立。但 autoplan 全程说它不知道你的 API 是按量计费还是订阅——如果是订阅制封顶,"token 是真痛点"这个前提本身要重估,那会改变要不要急着做降本批。这句只有你能回答。

Q3 — 收敛后的 roadmap 要不要用 /sdflow-roadmap 固化成 openspec/roadmaps/ 三件套?
它跨 6 阶段 10+ change,超单次 change 层级,正是该 skill 的定义域;固化时可顺带归并两条旧 roadmap 状态。你点头我就跑。