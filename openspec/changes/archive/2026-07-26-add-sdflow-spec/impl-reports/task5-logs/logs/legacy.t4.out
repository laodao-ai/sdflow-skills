实测了三处，先把两个**事实修正**摆出来（不是问题，是我上一轮写错的地方，我会改）：

**修正 1｜源文件必须自带 marker 行。** 实跑 `sync_principles.render()`：`block(src)` 直接把源文件内容当注入文本，marker 行**不是注入器加的**——真相源 `sdflow-init/assets/hack/skill-principles.md` 第 1 行就是 `<!-- sdflow:principles:start … -->`。所以 `ov-protocol.md` 是 **111 行含两行 marker**，不是我 tasks 3.1 写的「109 行正文」。

**修正 2｜`render()` 强制在块前后各插一个空行。** 实测输出：

```
site=design-voice → 取材
                          ← render 强插
<!-- sdflow:ov-protocol:start —— 说明 -->
NEW 1
<!-- sdflow:ov-protocol:end -->
                          ← render 强插
```
```

幂等 ✅、两个不同 marker 互不干扰 ✅，但这两个空行会**当场推翻 tasks 4.5 的「搬运保真 diff 必须为空」硬门**。

---

## Q1｜块① 的边界要不要把 ` ``` ` 围栏本身包进去？

现在协议正文整体活在一个 fenced code block 里（`SKILL.md:360` 开栏、`474` 闭栏），marker 落在栏内 ⇒ render 的两个空行也落在栏内，其中「闭栏前一个空行」尤其难看，且让保真 diff 恒不为空。

三条路，我查完的对比：

| 方案 | 空行落点 | 站点表位置 | 代价 |
|---|---|---|---|
| **(a) 块含围栏**（源第一行 = marker，第二行 = ` ``` `，倒二 = ` ``` `，末行 = end marker） | 落在**围栏外**的正常 Markdown 位置——本来就该有空行 | **必须移到围栏外**（成为普通 Markdown 列表/表格） | 站点表要重排版；块比现在多 2 行 |
| (b) 接受栏内空行 | 栏内两处空行 | 留栏内（现状位置） | 保真门要放宽成「忽略块前后各一空行」——**门被自己的实现细节磨钝了一格** |
| (c) 给 render 加 per-block padding 开关 | 可控 | 任意 | 动注入器核心的 `render()`——那 50 行含两个已踩过的坑（多块全量更新、`relpath` 不崩），为排版风险去改它，收益比最差 |

**我推荐 (a)**，三条依据：① 空行落到围栏外是 Markdown 的正常形态，保真门可以要求**严格为空**，不用为实现细节松口径；② 站点表移出围栏其实更对——它是「本层事实表」，给人和模型读的，本来就不该混在一段协议伪代码里；③ 零改注入器，风险最低。代价只有「站点表重排版」这一次性动作，而它本来就要新写。

这条会同时消掉 design 里 Open Questions 的第一条（「站点表放栏内还是栏外」）——它从「无所谓的排版偏好」变成了**有机械理由的决定**。

**要不要按 (a) 定？**

===META=== rc=0 sid=ed6c1a71-7aed-450c-aefd-aafc2dada45a cost=2.7816 dur_ms=152949 turns=11 err=False denials=[]
usage in=15 out=8470 cache_create=187669 cache_read=1386154
  model claude-opus-5: in=15 out=8470 cr=1386154 cc=187669 cost=2.7816
