<!-- sdflow:step1-broad-review v1 mode="simulated" -->

# 广审（Step1）— batch-triage-strategy

> **降级声明（显著呈现，不埋）**：本 change 为 5 文件纯 markdown 规则变更。gstack `/autoplan` 全流水线
> （CEO 10 段 / design 7 维 / DX 8 维 + 4 阶段 codex 双声）对其**严重不成比例**（产品野心/UI/DX 镜对批次判据规则近乎零信号），
> 违本 roadmap 自身「评审深度匹配 change」ethos。故 **autoplan 缩到广审本质**（scope-drift + 完成度 + eng 合理性），
> 由**主 session 原生**执行、读真码接地——**非** gstack autoplan pipeline 真跑，故 mode="simulated"（诚实标注，非伪装 native）。
> **冷独立层不砍**：真正评审重量在 Step2（对抗×2 + 接地×1 + outside-voice codex，全强度保留）。此为「广审其余镜缩减」，
> outside-voice 切片 Step2 会自跑补齐（守卫回落，非复用 autoplan 的 codex——因本次未真跑 autoplan）。

## 广审 findings（纳入 Step3 合并池）

- **B1〔scope-drift·低〕**：tasks 3.2「顺带刷新 consolidation-plan stale 状态（REC-1=gate-checkpoint-hardening、G7=sdflow-init-hardening 已归档）」——接地证实二者确已归档（`openspec/changes/archive/2026-07-05-gate-checkpoint-hardening`、`2026-07-06-sdflow-init-hardening`），stale 属实、刷新正当（同文件、低增量，BASE-18 fold 合理）。**守卫**：刷新 MUST 限于状态订正，勿借机重新 litigate REC-1/2/3 的合批设计（那是另一 scope）。

- **B2〔完成度·OK〕**：design D1-D7 与 specs 7 条 Requirement 双向可追溯——D1→三分类/硬边界、D2→同类Leg1/fail-closed(纯规则)、D3→聚合上限、D4→fail-closed、D5→consolidation二分、D6→bundle、D7→一项一commit。无孤儿决策、无未覆盖需求。完成度一致。

- **B3〔设计·中〕一项一 commit MUST × checkpoint-commit 的 `git add -A` 张力**：接地证实 `~/.sdflow/hack/checkpoint-commit.sh` 第 51 行为 `git add -A`（暂存全部）。若大扫除批实现时多个 item 的编辑**累积后**才调一次 checkpoint-commit，`git add -A` 会把多 item 打进一个 commit → **静默违反 D7「一项一 commit」**。故规则/spec MUST 明确：sweep **逐 item 执行 + 每 item 后立即 checkpoint**（不得攒批），否则 D7 的 revert 粒度保证在现有 checkpoint 语义下不成立。**建议 Step2 对抗镜深挖此执行契约。**

- **B4〔设计·OK〕大扫除批 vs consolidation-plan AND 门无冲突**：AND 门管相关合批（同 cap∧高耦合），大扫除批管正交琐碎——design 决策流三分（相关→AND / 正交琐碎→sweep / 其余→单开）是干净 partition，互补非重叠。无双重计数、无矛盾。

- **B5〔ROI·OK 但有前提〕纯规则未 ROI 抽空**：3 条硬 MUST（禁逻辑面/生成物隔离/一项一commit）+ worked example 给了具体牙齿，非「小心点」。接地证实 `retro/report.md`（tracked 生成物）、`INDEX.md`（`GENERATED by issues.py reindex`）确为生成物，「含生成物隔离」示例准确。**前提**：efficacy 依赖应用判据者的纪律（纯规则固有，design D4 已诚实标注无机械兜底）——非缺陷，是已知边界。

## 广审自动决策（登记，供设计门）
- 无需拍板项由广审新增。B3 升为 Step2 对抗镜重点核查项。
