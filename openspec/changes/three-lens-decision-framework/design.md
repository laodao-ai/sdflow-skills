# 设计 — 三镜决策框架焊进 workflow 源头

> **元说明**：本 change 的主题就是「决策必按三镜 + 定主次」。故下方每个 ADR 亲自用该框架收敛——既是设计、也是框架的第一个自证样例（dogfooding）。真相源 = 记忆 `decision-three-lens-framework.md`；参考样例 = review-tool-followups 的 ADR-0/1/2（已按三镜回填）。grill 五决策（2026-07-05）已定稿，本文档落盘。

---

## ADR-1：落点形态 —— 增强现有 BASE-12，不新增独立编号项

**背景**：三镜要在书面层（bundle 质量门）有落点。BASE-12 已是「备选方案记录 / ADR」槽，含「候选方案」「理由」「被否决方案」。三镜是往哪放？

**候选方案**（按三镜评估）：

| 方案 | 系统镜（耦合/单一源） | 用户镜（读 spec 的工程师） | 开发循环镜（未来维护） |
|---|---|---|---|
| **A. 挂进 BASE-12 现有槽**（选定） | 无新文件 / 新编号，规则集单一源、耦合最小 | 在既有「候选方案 / 理由」处就近看到三镜要求，认知连续 | 改决策规则只动一处 |
| B. 新增 BASE-30 独立项 | 三镜逻辑与 BASE-12 alternatives 重叠 → 双源 | 要跨项对照「BASE-12 写方案、BASE-30 写三镜」 | "改一处忘另一处"漂移面 +1 |
| C. 新增独立 `three-lens.md` 约束文件 | 引入新 bundle 文件 + INDEX 登记 + resolver 覆盖 | 又一处要查 | 最重维护面 |

**决策**：A —— 三镜评估法挂进 BASE-12「候选方案」（方案对比按三镜展开）、主次判定行挂进「理由」（末尾补一句主次判定）。

**定主次**：本问题核心是**避免双源 / 规则重叠**——**开发循环镜压倒**。理由：三镜框架若自己造成规则漂移就自相矛盾；BASE-12 的「候选方案 / 理由」本就是三镜的天然宿主，原地增强零新增维护面。用户镜（认知连续）与之同向、不冲突，故不构成 tradeoff。

**当前方案代价**：BASE-12 检查点行变长（可读性略降）；三镜与「最小可行 + 理想架构」两句要在同一槽内共存，措辞需拧清。

---

## ADR-2：强度 × 触发深度 —— 分两层，书面层只 TG-23 触发 MUST

**背景**：三镜是「每个决策都 MUST 写」还是有触发门？全量 MUST 会让琐碎决策也背三镜样板。

**候选方案**（按三镜评估）：

| 方案 | 系统镜（bundle 噪声/开销） | 用户镜（读者信噪比） | 开发循环镜（生成循环负担） |
|---|---|---|---|
| A. 每个决策都 MUST 写三镜（全量硬门） | 琐碎决策也写三镜段 → spec 噪声爆炸 | 读者被样板淹没 | 生成循环每步背样板，拖慢 |
| **B. 分两层**（选定） | 书面负担限定在 ≥2 方案的高杠杆决策 | 只在真正 ≥2 方案处见三镜，信噪比高 | 高杠杆处焊死、琐碎处轻 |
| C. 全 SHOULD（软建议） | 无强制 | 可能整段缺失 | 无门禁 = 形同虚设，退回私有记忆困境 |

**决策**：B —— **行为层**（私有记忆 `decision-three-lens-framework.md`）每个决策都用（自动决策 / 推荐 / 补主次判定）；**书面层**（BASE-12 + workflow.md G2 + code-review Step4）只在 **TG-23（≥2 合理方案 / 非显然设计）触发时 MUST 写**三镜 + 主次，不下沉到琐碎决策。

**定主次**：本问题核心是**框架要生效、但不能变成样板税**——**用户镜（信噪比）与开发循环镜（不拖慢）并重**。二者都指向"限定在高杠杆决策"；触发深度复用 TG-23 已有机制承载最省（不新造触发条件）。系统镜同向。故 B。

**当前方案代价**：判断"是否 ≥2 合理方案 / 非显然设计"由模型在 TG-23 处判——存在漏判把该写三镜的决策当琐碎跳过的风险；靠 spec-review / code-review 的对抗镜兜底。

---

## ADR-3：进程 —— 走独立 OpenSpec change + spec delta，不裸改源

**背景**：workflow bundle 改动够轻能直接改 assets，还是须走 propose → review → done？

**候选方案**（按三镜评估）：

| 方案 | 系统镜（防漂移） | 用户镜 | 开发循环镜（审计/兜底） |
|---|---|---|---|
| **A. 独立 change + spec delta**（选定） | spec delta 锚定行为契约，未来改 G2/Step4 措辞时 delta 对码核验挡漂移 | 无直接可感差异 | 留 propose→review→archive 审计 + 评审兜底 |
| B. 直接改 assets 源（轻量） | 无锚，改了就改了、下次谁改 G2 忘了三镜也没人拦 | 同上 | 快但无痕、无评审 |

**决策**：A（grill 决策⑤已拍板）。grill 成果直接喂本四件套，不重新发明。

**定主次**：本问题核心是**治理层改动会传导进此后每个决策的登记与裁决口径**，错了代价高、且极易随后续 workflow 编辑悄悄漂移——**系统镜（防漂移锚）压倒**。开发循环镜（审计 / 兜底）同向。用户镜中性。故 A。

**当前方案代价**：比裸改多一轮 spec-review + done 开销；但治理层改动值这一轮。

---

## 五处落点的精确编辑锚（实现依据）〔spec-review 完整性镜穷举校准：原 3 处 → 5 处〕

| 落点 | 文件 | 现状锚 | 改法 |
|---|---|---|---|
| ① BASE-12 | `sdflow-init/assets/workflow/spec-checklists/spec-quality-base.md`（BASE-12，行 31） | 「2-3 方案对比（含最小可行 + 理想架构）；关键决策按 ADR 结构落盘：背景 / 候选方案 / 决策 / 理由 / 当前方案代价」 | 「候选方案」评估法补三镜（系统/用户/开发循环）；「理由」补主次判定行；**TG-23 触发时 MUST 三镜 + 主次**，否则 SHOULD。**三镜为新挂入 ADR 结构、非替换旧串**（此文件无「两方后果」精确串，勿用 grep-replace 误判无需改） |
| ② G2 | `sdflow-init/assets/workflow/workflow.md`（行 72 设计门 + 行 83 G2） | 「决策登记进报告（选项 + 推荐 + **两方后果**）」 | →「选项 + 推荐 + **三面后果（系统 / 用户 / 开发循环）+ 主次判定**」；行 72 设计门行同步 |
| ③ code-review | `sdflow-code-review/SKILL.md`（frontmatter 7-8 / 导语 30 / Step4 行 96 / 台账 143-144） | 「≥2 方案有把握自动选推荐（**记理由**）」；台账「附理由」/「T10复核」行 | 「记理由/附理由」→「按**三镜 + 主次**记」；台账行补主次判定。**判据词「有把握」不动**（T10 对齐 defer，见 Q1） |
| ④ spec-review〔F1/CV1〕 | `sdflow-spec-review/SKILL.md`（frontmatter 8 / 正文 24 / TENSION 77 / 格式块 89） | 「选项 + 推荐 + **两方后果**」/ TENSION「两方视角 + 推荐 + **后果**」/ 格式块「选项A/B + 推荐 + **各自后果**」 | 全部 → 三面后果 + 主次判定。**决策登记区实际执行入口，内联硬编码须改**，否则归档后主 spec 与发布产品矛盾 |
| ⑤ ship 台账〔完整性镜〕 | `sdflow-ship/SKILL.md`（行 23 T10 台账） | 「T10复核: <方案> \| 对抗镜结论 <通过/证伪> \| <一句理由>」（与 code-review:144 同串） | 随 code-review 台账同步补主次判定，防两处台账格式不一致 |

## spec delta（防漂移）

`specs/spec-workflow/spec.md` 两条 MODIFIED：
- **评审决策登记进报告**：后果字段「各分支后果」→「三面后果（系统/用户/开发循环）+ 主次判定」；补 TG-23 深度门 clause + code-review 侧 scenario。**「≥2 合理方案」（命中 TG-23，走三镜）与「核验不了的事实」（Q2 事实核验，不走三镜）分列**〔CV4：TG-23 定义仅 ≥2 方案，勿把事实核验混标 TG-23〕。
- **outside-voice tension 不静默采纳**：TENSION 条目「两方视角 / 后果」→「两方视角 + 三面后果 + 主次判定」；判据词用中性「自动裁决」不引入 T10/「有客观判据」新词，避免与本 change 纯三镜 scope 冲突〔F1 内部矛盾修复〕。

## 部署纪律（实现必做）

改的是 `sdflow-init/assets/workflow/` 权威源与自制 skill——**须在开发 checkout 跑一次 `bash setup.sh`** 才让全局 canonical（`~/.sdflow/workflow`）跟上、测得到（CLAUDE.md dev/runtime 纪律）。测完 / 合并后在运行 checkout 重跑 setup 还原。**不新增 / 删除 bundle 规则文件**，故无 INDEX 规则块同步。

## 不在本次范围内

- 行为层记忆内容重写（仍是真相源，只搬运）。
- 既有 ADR（review-tool-followups）回填——已完成，作样例。
- 触发漏判的自动化检测（靠对抗镜兜底，未来另议）。
- **docs/ 可视化镜像刷新**〔spec-review F3〕：非权威源、量大，延后另 change 刷（显式声明，非静默留漂移）。
- **「有把握」→ T10 三级协议对齐**〔spec-review F2/CV2，决策登记 Q1 采 B〕：pre-existing 债（主 spec:48 已 T10、code-review:96 仍「有把握」），非 T46 引入；纯三镜单一职责，defer 进 todolist。
- **trigger-catalog「≥2 合理方案」判例补充**〔对抗镜 X2 提示，降 TG-23 漏判漂移〕：可选优化，defer。
