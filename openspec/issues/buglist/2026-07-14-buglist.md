# 2026-07-14 Buglist

> 来源：<未注明>
> 创建日期：2026-07-14

## 状态总览

| ID | 模块 | 问题摘要 | 优先级 | 状态 | 时间 | 关联Change | 批次 |
|----|------|----------|--------|------|------|------------|------|
| B6 | `sdflow-architecture/scripts/sad_schema.py` | contract 扫描剥 fence，而模版要求 contract 写 fence 内 ⇒ 真实 SAD 恒扫出 0 条，sad_lint 的 contract 不变式校验从未触发 | P1 | FIXED | 22:36 | add-sdflow-devenv |  |

---

## B6: contract 扫描剥 fence，而模版要求 contract 写 fence 内 ⇒ 真实 SAD 恒扫出 0 条，sad_lint 的 contract 不变式校验从未触发

| 属性 | 值 |
|------|------|
| 模块 | `sdflow-architecture/scripts/sad_schema.py` |
| 优先级 | P1 |
| 状态 | FIXED |

**关联文档**：`openspec/changes/add-sdflow-devenv/design.md`

**现象**：mqtt-console 的真实 SAD 有 44 处 contract[...]（全在 ``` fence 内，因为 sad-template.md 就是这么示范的），但 scan_contract_tags() 返回 0 条 ⇒ sad_lint 的 _check_contract_invariants 恒空转、devenv_lint 的 SAD contract 差集恒空（而空集渲染出来跟「全都覆盖了」一模一样）。sdflow-architecture 自己的测试全绿——因为 conftest 的 fixture 恰好把 contract 行写在 fence 外。

**根因**：<待分析>

**修复方案**：
- <待补充>

**影响范围**：<待评估>
