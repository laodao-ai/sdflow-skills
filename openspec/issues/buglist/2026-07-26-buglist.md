---
sdflow-issues:
  schema: 1
  pool: bug
  mode: canonical
  items:
    B21: {"module":"sdflow-init/assets/hack/outside-voice-job.py: acquire_reservation","summary":"slot-limit 计数 TOCTOU：三方并发占坑时全部自我回退，本可成功的组合被整体饿死且禁 fallback","priority":"P3","status":"PROPOSED","time":"09:06","change":"enable-codex-background-outside-voice","batch":"enable-codex-background-outside-voice"}
    B22: {"module":"sdflow-issues/scripts/migrate_legacy.py","summary":"迁移写入与自动 reindex 不在同一 recorder lock 事务内，中途被抢锁会留下已永久写入的 overlay","priority":"P3","status":"PROPOSED","time":"09:06","change":"enable-codex-background-outside-voice","batch":"enable-codex-background-outside-voice"}
---
# 2026-07-26 Buglist

> 来源：cold code-review 对抗镜（并发竞态角度），实测复现
> 创建日期：2026-07-26

<!-- sdflow-issue-block:start id=B21 -->
## B21: slot-limit 计数 TOCTOU：三方并发占坑时全部自我回退，本可成功的组合被整体饿死且禁 fallback
> slot-limit 计数 TOCTOU：三方并发占坑时全部自我回退，本可成功的组合被整体饿死且禁 fallback

**关联文档**：`openspec/changes/enable-codex-background-outside-voice/design.md`

**现象**：3 个并发 dispatch 对不同 site 各自 O_CREAT|O_EXCL 建 reserve（互不冲突、全部成功），随后各自 os.listdir 数坑；若三次 listdir 都发生在三次 create 之后，三者都看到 len==3>2 于是全部 os.unlink 自我回退 ⇒ 0 个存活预留，且返回 state=slot-limit / fallback_allowed=False（硬编码），调用方连同族 fallback 都被禁。实测：真多进程 + Barrier 强制同时起步、30 轮 trial，12/30 轮出现 0 个存活预留，从未稳定收敛到「恰好 2 个赢」。复现脚本 /private/tmp/claude-501/-Users-cheneyzhao-Documents-04-sdflow-skills/a137a171-20d7-4ebd-af7b-fcf67dcc1281/scratchpad/adv-race-7x2q9k/race_slot.py

**根因**：<待分析>

**修复方案**：
- <待补充>

**影响范围**：<待评估>
> 2026-07-26 状态：OPEN → PROPOSED
<!-- sdflow-issue-block:end id=B21 -->

<!-- sdflow-issue-block:start id=B22 -->
## B22: 迁移写入与自动 reindex 不在同一 recorder lock 事务内，中途被抢锁会留下已永久写入的 overlay
> 迁移写入与自动 reindex 不在同一 recorder lock 事务内，中途被抢锁会留下已永久写入的 overlay

**关联文档**：`openspec/changes/enable-codex-background-outside-voice/design.md`

**现象**：overlay 写入在锁内完成后立即释放锁（migrate_legacy.py:363），随后才另起进程做 reindex（:370）。其他 recorder 可在这个间隙插入或占锁，使 reindex 失败；而此时多个 overlay 已按文件依次永久写入，错误信息本身也承认「migration 已写入」。

**根因**：<待分析>

**修复方案**：
- <待补充>

**影响范围**：<待评估>
> 2026-07-26 状态：OPEN → PROPOSED
<!-- sdflow-issue-block:end id=B22 -->
