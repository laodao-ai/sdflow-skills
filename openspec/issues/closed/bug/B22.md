---
id: "B22"
pool: "bug"
status: "WONTFIX"
priority: "P3"
type: null
date: "2026-07-26"
source_change: "enable-codex-background-outside-voice"
module: "sdflow-issues/scripts/migrate_legacy.py"
summary: "迁移写入与自动 reindex 不在同一 recorder lock 事务内，中途被抢锁会留下已永久写入的 overlay"
resolved_by: null
closed_date: "2026-08-03"
closed_reason: "migrate_legacy 是一次性操作且已全量完成，不会再跑"
---
## B22: 迁移写入与自动 reindex 不在同一 recorder lock 事务内，中途被抢锁会留下已永久写入的 overlay
> 迁移写入与自动 reindex 不在同一 recorder lock 事务内，中途被抢锁会留下已永久写入的 overlay

**关联文档**：`openspec/changes/enable-codex-background-outside-voice/design.md`

**现象**：overlay 写入在锁内完成后立即释放锁（migrate_legacy.py:363），随后才另起进程做 reindex（:370）。其他 recorder 可在这个间隙插入或占锁，使 reindex 失败；而此时多个 overlay 已按文件依次永久写入，错误信息本身也承认「migration 已写入」。

**根因**：<待分析>

**修复方案**：
- <待补充>

**影响范围**：<待评估>
> 2026-07-26 状态：OPEN → PROPOSED
> 2026-08-03 状态：PROPOSED → WONTFIX（migrate_legacy 是一次性操作且已全量完成，不会再跑）

> [迁移自批次 enable-codex-background-outside-voice] 原计划: <待填>
