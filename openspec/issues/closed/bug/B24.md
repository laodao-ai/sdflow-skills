---
id: "B24"
pool: "bug"
status: "FIXED"
priority: "P2"
type: null
date: "2026-08-06"
source_change: "refactor-roadmap-internalize-deps"
module: "hack/tests/test_subprocess_encoding_contract.py"
summary: "扫描口径受 .claude worktree 污染 + 硬编码阈值 200 已失真(实测189,当前即红)。两个缺陷叠加导致同一 commit 可既绿又红：_python_files() 排除清单不含 .claude，本机存在 agent worktree 时 3 份仓库副本被一并扫入、sites 计数约翻 4 倍越过阈值判绿；worktree 清理后暴露真实值 189 < 200 判红。实证(refactor-roadmap-internalize-deps Task 6 编排层亲跑)：worktree 在→4 passed，git worktree remove 后→1 failed。修法：①排除清单补 .claude（治口径漂移）②删掉硬编码 200、改为让脚本自报数量或锚定不变量（治阈值失真，同 CLAUDE.md「硬编码数量修法=删掉数字让脚本自己报」）。非本 change 引入：merge-base..HEAD 的 .py 改动为 0。"
resolved_by: "main"
closed_date: "2026-08-12"
closed_reason: null
---
> 2026-08-12 状态：OPEN → FIXED（排除清单补 .claude 防 worktree 口径污染；阈值 120 作为防塌陷哨兵已合理（当前 195 sites）；4/4 passed）
