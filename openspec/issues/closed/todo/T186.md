---
id: "T186"
pool: "todo"
status: "DONE"
priority: null
type: "代码质量"
date: "2026-07-01"
source_change: "fix-design-gate-freshness-proxy"
module: "sdflow-ship/ship_gate.py"
summary: "merge 帧在 live 路径取不到文件列表 → design 域逐 parent 豁免分支不可达（evil-merge 面）"
resolved_by: null
closed_date: "2026-07-01"
closed_reason: null
---
## T186: merge 帧在 live 路径取不到文件列表 → design 域逐 parent 豁免分支不可达（evil-merge 面）
> merge 帧在 live 路径取不到文件列表 → design 域逐 parent 豁免分支不可达（evil-merge 面）

**关联文档**：`openspec/changes/fix-design-gate-freshness-proxy/design.md`

**动机**：Task1 Spec 轴评审实测：git log --name-only 对 merge 提交默认不输出 diff，merge 帧只有 sha+subject 无文件行 ⇒ is_stale 的 tasks_touched 对 merge 帧恒 False ⇒ design_frame_exempt 的逐 parent 分支从 is_stale 不可达，仅单测层覆盖。属既有行为（evil merge 单改 design.md 今天也不判失鲜），非本 change 引入。

**思路**：MUST NOT 在此处补 --diff-merges——会扩大 BR-6 护栏面（头注释承诺 merge 内部提交逐一枚举，改 diff-merges 会改变 merge 场景失鲜语义）。建议 Task2 打开勾选框豁免后，先复核 evil-merge 面的真实暴露度再定修法。

**备注**：Task1 双轴审 Spec 轴 Minor，评审方自己建议不在本票修。
> 2026-07 状态：OPEN → DONE（58cef16）
