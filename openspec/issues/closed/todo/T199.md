---
id: "T199"
pool: "todo"
status: "WONTDO"
priority: null
type: "基础设施"
date: "2026-07-01"
source_change: "harden-gate-git-layer"
module: "CLAUDE.md / 开发环境"
summary: "CLAUDE.md 写的 `pytest` 与默认 python3 在非交互 shell 均不可用，实际须用 /usr/bin/python3 -m pytest"
resolved_by: "main"
closed_date: "2026-08-05"
closed_reason: "本机PATH环境事实(pytest解到无pytest的~/.local/bin),非文档错误;CLAUDE.md面向标准环境,memory已记录"
---
## T199: CLAUDE.md 写的 `pytest` 与默认 python3 在非交互 shell 均不可用，实际须用 /usr/bin/python3 -m pytest
> CLAUDE.md 写的 `pytest` 与默认 python3 在非交互 shell 均不可用，实际须用 /usr/bin/python3 -m pytest
> 2026-07 状态：OPEN → PROPOSED

> [迁移自批次 harden-gate-git-layer] 原计划: <待填>
> 2026-08-05 状态：PROPOSED → WONTDO（本机PATH环境事实(pytest解到无pytest的~/.local/bin),非文档错误;CLAUDE.md面向标准环境,memory已记录）
