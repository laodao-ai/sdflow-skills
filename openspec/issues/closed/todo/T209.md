---
id: "T209"
pool: "todo"
status: "WONTDO"
priority: null
type: "功能增强"
date: "2026-07-01"
source_change: "dedupe-issues-scripts-shared-layer"
module: "sdflow-issues/scripts/issues.py"
summary: "move --to-pool 跨池搬运命令（误判落错池的机械恢复路径）"
resolved_by: "main"
closed_date: "2026-08-04"
closed_reason: "三仓历史零误判，手动 delete+re-add 成本可接受，不为从未发生的场景建命令"
---
## T209: move --to-pool 跨池搬运命令（误判落错池的机械恢复路径）
> move --to-pool 跨池搬运命令（误判落错池的机械恢复路径）

**关联文档**：`openspec/changes/dedupe-issues-scripts-shared-layer/design.md`

**备注**：AD-6/R7 显式 defer：触发面塌缩后 bug↔todo 分池由模型在 skill 内按坏了没判，NL 骑墙输入可能误判落错池，拿错前缀(B↔T)/粒度/schema/词表；当前 CLI 无 move/reclassify，纠正须手删+重 add、丢 ID 与历史。move --to-pool 应换前缀+粒度文件+schema+保 provenance 做机械恢复。nice-to-have，属将来另开 CLI change，非本 change 阻断项。
> 2026-07 状态：OPEN → PROPOSED

> [迁移自批次 dedupe-issues-scripts-shared-layer] 原计划: <待填>
> 2026-08-04 状态：PROPOSED → WONTDO（三仓历史零误判，手动 delete+re-add 成本可接受，不为从未发生的场景建命令）
