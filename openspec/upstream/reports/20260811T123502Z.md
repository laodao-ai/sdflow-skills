# 上游追踪分诊报告 20260811T123502Z

生成时间：2026-08-11T12:32:53Z
facts 来源：openspec/upstream/.facts/20260811T123253Z.json

## Seed 分诊条目（仅首轮报告含此节）

- **T245**：fix loop 熔断前插入一轮「换更强模型 fresh implementer 重试」，对齐 superpowers
  subagent-driven-development 的第4-5轮机制。与 T246 共享同一个前置人工决定——是否解除
  design D8（matt-workflow-integration）把 implementer 档位钉死为 mid 档的试点期变量控制
  约束；该决定未定前两条均分诊为**观望**。
- **T246**：按任务复杂度动态选 implementer 档位，对齐 superpowers subagent-driven-development
  的 Model Selection 复杂度信号。同上，共享 D8 mid 档钉死解除的前置决定，分诊为**观望**。
- **T267**：建立 python.md checklist domain 承接 Async/Sync 混用等 Python 专属检查点
  （gstack Pass-2 遗留、absorb-gstack-review 因归属 domain 不存在而 defer）。无共享前置决定、
  scope 已在此前评审中定义清楚（decision-memo D1 明确记录为「读法 B 候选」——本轮 p3 有意不
  顺手做，理由是 scope 纪律而非价值存疑），分诊为**吸收候选**（已在池内以 T267 追踪，本轮
  无需重新 add）。

## gstack

**采集状态**：ok（HEAD `94993f74012782fd94416dd44b8314f6363a13a4`）

本地锚源（`~/.skills/gstack`）HEAD 为 `960c3a8d6c4d14cb4c5e551a8847f8ec7c4267df`
（v1.60.2.0），首轮以此为天然锚，`960c3a8..94993f7` 区间非空——**确认真 delta**（非「当前态
即基线」零 delta 分支）。

- `94993f74012782fd94416dd44b8314f6363a13a4` v1.61.0.0 fix wave: guards failing open /
  silent failures (9 fixes, 4 community PRs absorbed) (#2472) — **分诊：不吸**（整体）。
  理由：单条 squashed 发布提交打包 9 个 gstack 自有 skill/hook 的 guard 修复，逐项与本仓
  同类面对照如下（`git show` 取内容辅助判断，非解析上游语义，仅人工阅读 CHANGELOG.md 与
  commit body）：
  - AskUserQuestion 在 CC 2.1.89+ 被孤立（`question-preference-hook` 的
    `permissionDecision:'defer'` 语义漂移为「暂停待外部恢复」）→ **不吸**：本仓无
    question-preference 类 hook；已核对 `sdflow-init/assets/hooks/ff0-branch-guard.py`
    （本仓唯一 PreToolUse 判定型 hook），其未判定路径本就只输出 `additionalContext`、
    从不设置 `permissionDecision`（`ff-generation-constraints.md` 明文约束），已站在正确
    一侧，无需吸收此修复。
  - `/careful` 链式 `rm` 绕过（正则只看链中最后一个 `rm` 目标）→ **不吸**：本仓无同类
    「按正则白名单放行危险命令链」的 guard 代码，无对应修补面。
  - `/context-restore` 误读兄弟 worktree 的 checkpoint → **不吸**：本仓无 context-restore
    类 skill。
  - `/sync-gbrain` drift 重复注册 → **不吸**：本仓不集成 gbrain。
  - developer-profile 会话计数重复计入 → **不吸**：本仓无 developer-profile 功能。
  - one-way-door 凭据关键词网（复数形式 + 运行时接线）→ **不吸**：本仓无 one-way-door /
    凭据拦截类功能。
  - design CLI 数值 flag 静默 NaN（`--count`/`--retry`/`--timeout` 改为 loud 失败）→
    **不吸**：已 grep 本仓全部 `add_argument` 调用，数值型参数均省略或统一走
    `argparse` 默认字符串/choices 校验，无「非法数值静默吞掉」的等价缺陷面；巡查成本已付，
    结论是无同类缺陷，非「未查」。
  - 「closed as already fixed with receipts」三条（#1965/#1950/#1964）→ 不适用，均为
    gstack 自身既有修复的回溯记录，非本仓可吸收面。

（无采集降级）

## matt

**采集状态**：ok（bare 缓存 HEAD `84fdeffd12f2ee307994d1eb6feb48173b6e0502`）

首轮无持久锚 ⇒「无锚 ⇒ 当前上游态即基线」，`commits` 为空（零 delta，非降级）。
`installed_skills`（本地 `.skill-lock.json` 快照，仅记录，无历史对比对象）：

| skill | skillFolderHash |
|---|---|
| grill-me | `8320e7b8` |
| grill-with-docs | `21adba95` |
| handoff | `3f7df8e7` |
| improve-codebase-architecture | `e5beae48` |
| prototype | `046ef7c3` |
| setup-matt-pocock-skills | `3676b312` |
| tdd | `57e1bee8` |
| triage | `3e7a4915` |
| domain-modeling | `028a0e44` |
| grilling | `0ace40ac` |
| ask-matt | `affab267` |
| code-review | `9df6fdac` |
| codebase-design | `c5dfc023` |
| diagnosing-bugs | `0ecacff0` |
| implement | `aa659906` |
| research | `06c848c1` |
| teach | `3b6a4245` |
| to-spec | `b2f449aa` |
| to-tickets | `b6224517` |
| wayfinder | `83a96a44` |
| writing-great-skills | `c84411b3` |
| claude-handoff | `4da13972` |
| design-an-interface | `59d5eca0` |
| edit-article | `d3e748a6` |
| git-guardrails-claude-code | `f02ae7c2` |
| loop-me | `1cec9dea` |
| migrate-to-shoehorn | `ad722b79` |
| obsidian-vault | `1e814d16` |
| qa | `3eed5beeb1` |
| request-refactor-plan | `b0617c9c` |
| resolving-merge-conflicts | `b601c904` |
| scaffold-exercises | `ef19ca90` |
| setup-pre-commit | `5e6e9301` |
| ubiquitous-language | `483fe10b` |
| wizard | `651aef46` |
| writing-beats | `28c4a84a` |
| writing-fragments | `ae1380ae` |
| writing-shape | `2ca73419` |

无 delta 可分诊（首轮建锚）。下一轮起 `advance` 已建的锚会推进，届时对比才产生真实
commit delta。

## superpowers

**采集状态**：ok（marketplace HEAD `920824c3e9509890fbec03ba6097014222393022`，
`installed_version`=6.2.0）

首轮无持久锚 ⇒「无锚 ⇒ 当前上游态即基线」，`commits`/`source_sha_sequence` 均为空
（零 delta，非降级）。已安装版本 6.2.0，无历史 delta 可分诊。

## OpenSpec

版本对照：已安装 `1.8.0` vs registry 最新 `1.8.0`（无版本差）。

schema drift（对比基线 = fork 目录 `sdflow-init/assets/schemas/sdflow-spec-driven/`
vs 已安装版 `@fission-ai/openspec` 的 `schemas/spec-driven/`，非 registry 最新版；
零解析逐文件字节级 sha256 对比）：
- changed: `schema.yaml`、`templates/proposal.md`
- added: 无
- removed: 无

人工核对两文件的实际 diff（非解析，直接 `diff` 读内容）：混合两类差异——
1. **本仓既有意图性定制**（预期差异，非新信号）：`name: sdflow-spec-driven`、
   proposal 阶段的 `<!-- sdflow:delegation:start -->` STOP 指令块、行折行格式差异。
2. **上游新增能力，fork 尚未跟进**（真实 delta）：上游模板把 capability 标识从扁平
   `<name>`（`user-auth`）改为支持嵌套路径的 `<capability-path>`（如
   `identity/user-auth`），并把 Modified Capabilities 一节的匹配口径从
   「existing spec names」改为「exact existing path under openspec/specs/」——
   即 OpenSpec 官方 schema 已原生支持嵌套 capability 目录组织，本仓 fork 的
   `schema.yaml` / `templates/proposal.md` 措辞仍停留在扁平命名假设。

**分诊：吸收候选**——第②类差异是本仓 fork 该跟进的真实上游能力升级（嵌套 capability
路径组织对多模块项目有实际价值，且改动范围小，只涉及两个模板文件的措辞）。预生成入池
命令：

```bash
python3 sdflow-issues/scripts/issues_v2.py add --pool todo --json '{
  "module": "sdflow-init/assets/schemas/sdflow-spec-driven/schema.yaml + templates/proposal.md",
  "summary": "跟随上游 OpenSpec CLI（@fission-ai/openspec 1.8.0）schema 模板变更：capability 标识从扁平 <name> 改为支持嵌套路径的 <capability-path>（如 identity/user-auth），Modified Capabilities 匹配口径改为精确既有路径；更新本仓 fork 的 schema.yaml + templates/proposal.md 措辞，保留既有 sdflow:delegation STOP 指令块不动",
  "type": "上游吸收",
  "source_change": "sdflow-upstream-watch"
}'
```

（无采集降级）

## 分诊摘要

共 2 条 delta 具备可分诊内容（gstack 1 条 squashed 发布提交、OpenSpec 1 处 schema
drift；matt/superpowers 首轮零 delta 仅建锚）；吸收候选 1 条（OpenSpec schema 嵌套
capability 路径）、观望 2 条（seed T245/T246，D8 前置决定未解除）、不吸 1 条（gstack
v1.61.0.0 fix wave，逐项核对无本仓等价缺陷面）；0 源降级（gstack/matt/superpowers/
OpenSpec 均 `status: ok`）。T267 作为独立 seed 条目分诊为吸收候选（已在池内追踪，非
本轮新增）。
