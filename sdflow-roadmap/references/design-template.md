# design.md 模板（整体设计 — HOW + WHY）

用途：描述项目"怎么做 + 为什么这么做"。架构决策的权威源。

使用方法：把本模板内容 Write 到 `openspec/roadmaps/<name>/design.md`，按项目内容替换占位符、删除不适用的节。

**关键原则**：每个架构决策必须同时回答"选什么"（HOW）和"为什么不选别的"（WHY）。后者往往比前者重要，因为未来争议时是 WHY 能终结讨论。

---

# <项目名> 整体设计

> 版本：v<version>（<yyyy-mm-dd>）
>
> 相关文档（全部位于 `openspec/roadmaps/<name>/` 下）：
> - 实施路线图：`roadmap.md`
> - 任务日志：`task-log.md`

## 需求与目标态

<!-- 伸缩判据（按项目类型决定必填/可选/追加小节，见下文各小节注释）：
     - 工作流型 / 重构型（内部工具、技术重构、基础设施）：「痛点」「目标态判据」「验收门槛」「Non-Goals」四节必填，「需求清单/优先级」可选
     - 产品型（含外部用户/商业目标）：在工作流型基础上追加「受众」「功能取舍」两节必填，NFR 为可选占位
     - 混合/探索型（目标态本身待发现，如技术可行性探索类项目）：「目标态判据」走兜底——允许写"待第 N 阶段验证"类具名占位（指明缺什么信息、何时补齐），MUST NOT 为凑格式硬造数字或写空泛「TBD」〔spec-review-amendment SR-13〕
     本节承接旧 requirements.md 的核心职能，但**不占用**下文 `## N.` 数字编号序列（无编号章名）——规避历史「design §N」位置引用随内容增删级联位移〔SR-15〕。 -->

### 痛点

<!-- 工作流/重构型必填。列出现状的结构性问题，一句话一条 -->
1. <痛点 1>
2. <痛点 2>

### 目标态判据

<!-- 工作流/重构型必填。做完后如何判断"对了"——SHALL 是可证伪的判据，不是愿景式描述。
     混合/探索型兜底：判据本身待发现时写具名占位，例如"性能门槛：待第 2 阶段实测数据定，不早于压测硬造数字"——
     指明缺什么信息、何时补，MUST NOT 写空泛「TBD」或编造未经验证的数字〔SR-13〕 -->
- <判据 1>
- <判据 2>

### 验收门槛

<!-- 工作流/重构型必填。承接旧 requirements §6 阶段级验收标准的职能，可用表格槽；
     混合/探索型同上走具名占位兜底 -->
| 验收项 | 门槛 |
|---|---|
| <验收项 1> | <门槛，或具名占位「待 X 确定」> |

### Non-Goals

<!-- 工作流/重构型必填。每条排除 SHALL 附可证伪假设——即"什么条件出现会让这条排除失效"，
     MUST NOT 写空泛的「超出范围」 -->
- **<排除项 1>** — <可证伪假设：什么情况下这条会被推翻>
- **<排除项 2>** — <可证伪假设>

### 需求清单 / 优先级（可选）

<!-- 可选占位槽。仅当需要按优先级罗列具体需求条目时启用（如产品型多方评审场景）；
     简单工作流型项目通常上面四节已够，用不到时删除本节 -->
- P0：<需求>
- P1：<需求>

<!-- 以下两节仅产品型项目（含外部用户 / 商业目标）必填；工作流/重构型项目删除 -->

### 受众

<!-- 产品型必填。目标用户是谁、他们的核心诉求是什么 -->

### 功能取舍（做 / 不做）

<!-- 产品型必填。In-scope / Out-of-scope，负面定义能压缩后续讨论成本 -->

**做**：
- <范围 1>

**不做**：
- **<不做的事 1>** — <理由>

<!-- NFR（性能/可用性/安全/成本等）为可选占位，产品型项目视需要启用，工作流型通常不需要：
### NFR（可选）
| 维度 | 目标 |
|---|---|
| 性能 | <指标，或具名占位> |
-->

## 1. 架构概览

### 1.1 最终态拓扑（最后一阶段完成后）

```
<ASCII 图：最终形态的组件关系、数据流、外部依赖、边界>
```

### 1.2 演进路径（每阶段的架构形态）

```
阶段 1:
  <架构示意>

阶段 2:
  <变化>

阶段 N (最终):
  <同 1.1>
```

## 2. 技术栈

| 层 | 选型 | 版本/模式 | 作用 |
|---|---|---|---|
| <SSG/运行时/DB/CI...> | <具体选型> | <version> | <作用> |

## 3. 架构决策

每个决策含"决策 / 替代方案 / 选择理由"三部分。替代方案作为考古证据保留，用于未来重新讨论时的决策回溯。

### 决策 1：<决策标题>

**决策**：<一句话决定>

**替代方案**：
- **A. <候选 A>** → <为什么否决 / 缺点>
- **B. <候选 B>** → <为什么否决 / 缺点>
- **C. <候选 C>** → <为什么否决 / 缺点>

**选择理由**：
- <理由 1>
- <理由 2>

<!-- 视项目复杂度，决策数量建议 3-9 个。
     少于 3 可能思考不够深，超过 10 可能决策颗粒度太细。 -->

### 决策 2：<决策标题>

<!-- 同 决策 1 -->

### 决策 N：...

## 4. 目录结构

### 4.1 <仓库/项目> 目录结构

```
<目录树>
```

### 4.2 <部署/运行时> 结构（如适用）

```
<目录树>
```

### 4.3 文档 / 规划位置

```
<当前 roadmap 相关文档在主仓内的位置>
```

## 5. 数据流

### 5.1 <主要数据流 1>（例：写作 → 发布）

```
<ASCII 图或分步描述>
```

### 5.2 <主要数据流 2>

### 5.3 <特殊场景数据流>（例：LLM 爬取、定时任务）

## 6. 关键配置规范

### 6.1 <配置文件 1>（示意，完整配置见 roadmap 阶段任务）

```<lang>
<配置片段>
```

### 6.2 <配置文件 2>

## 7. 安全模型

### 7.1 <密钥/身份策略>

| 密钥 | 用途 | 权限 | 约束 |
|---|---|---|---|
| <密钥 1> | | | |

### 7.2 Secrets 管理

### 7.3 <其他安全基线>

## 8. 风险、权衡与回滚

### 8.1 风险清单

| 风险 | 级别 | 缓解 |
|---|---|---|
| **<风险 1>**：<一句话描述> | 高/中/低 | <缓解措施> |

### 8.2 关键权衡（明确接受的代价）

<!-- 把"刻意接受的代价"显式写出，避免未来抱怨"当初怎么没考虑这个" -->

**<权衡 1>：<选择 X 意味着接受 Y>**。<为什么接受>。

### 8.3 回滚与应急

| 故障场景 | 回滚动作 | 预估恢复时长 |
|---|---|---|
| <场景 1> | <动作> | <时间> |

### 8.4 作废处置（如有旧方案）

- <旧文件路径> → <归档/删除动作>

## 9. 规范层契约（与 OpenSpec specs 的映射）

<!-- 如果本 roadmap 对应的 OpenSpec 变更产出了 capability spec，在这里列映射 -->

本 roadmap 配套的 OpenSpec 变更定义了以下 capability（归档后进入 `openspec/specs/`）：

| Capability | 覆盖 | 需求数 |
|---|---|---|
| `<capability-name>` | <覆盖范围摘要> | <N> |

每个需求附"当/那么"场景验收，作为未来实施变更的验收标准。

## 10. 已决议档案（Q&A）

<!-- 记录讨论过程中最后一轮 Open Questions 的终局决策。
     保留"原问题 + 最终决策 + 理由"供未来追溯。 -->

### Q1 ✅ <问题标题>

**原问题**：<当时讨论的焦点>

**决策**：<最终选择>

**理由**：
- <理由 1>
- <理由 2>

### Q2 ✅ ...

## 11. 未决 / 可延后事项

<!-- 明确标注还没决或可延后的事项，避免"假装决完了" -->

- **<事项 1>**：<为什么延后 / 什么时候决>
- **<事项 2>**：<同上>
